[tool.poetry]
name = "jnu-grade-checker-guard"
version = "1.0.0"
description = "暨南大学成绩监控与GPA计算工具"
authors = ["Cloer"]

[tool.poetry.dependencies]
python = "^3.11"
playwright = "^1.44"
httpx = "^0.27"
click = "^8.1"
loguru = "^0.7"
cryptography = "^42.0"
pydantic-settings = "^2.3"
win11toast = "^0.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
