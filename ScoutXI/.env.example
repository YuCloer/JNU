# Register a server-side token at football-data.org, then set it only in your local environment.
# Never place this value in static/app.js or any browser-visible file.
FOOTBALL_DATA_API_TOKEN=replace_with_your_token
# Preferred provider for the five major leagues.  This is server-only too.
API_FOOTBALL_KEY=replace_with_your_api_football_key
